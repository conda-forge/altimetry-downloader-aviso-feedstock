schema_version: 1

context:
  version: "0.5.0"
  python_min: "3.11"

package:
  name: altimetry-downloader-aviso
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/altimetry-downloader-aviso/altimetry_downloader_aviso-${{ version }}.tar.gz
  sha256: 1f68ff79df98efeef897ca1ffc1af8de91c35a32fe47a70f515ffe2f98997e97

build:
  noarch: python
  number: 0
  python:
    entry_points:
      - altimetry-downloader-aviso = altimetry_downloader_aviso.cli:app

  script:
    - python -m pip install . -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
    - setuptools-scm
  run:
    - numpy
    - python >=${{ python_min }}
    - requests
    - typer
    - siphon
    - rich
    - files_collections >=2
    - pyyaml
    - pydantic
    - h5netcdf
    - netCDF4
    - xarray

tests:
  - python:
      pip_check: true
      imports:
        - altimetry_downloader_aviso
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: "Altimetry Downloader Aviso — Browse and download altimetry data from Aviso"
  license: Apache-2.0
  homepage: https://github.com/robin-cls/aviso
  repository: https://github.com/robin-cls/aviso
  license_file: LICENSE

extra:
  recipe-maintainers:
    - annesophie-cls
    - robin-cls
